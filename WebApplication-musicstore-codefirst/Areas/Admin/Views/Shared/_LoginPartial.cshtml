@*
    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("Logout", "Login", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
            @Html.AntiForgeryToken()

            <ul class="nav navbar-nav navbar-right">
                <li>
                    @Html.ActionLink("Hello " + User.Identity.Name + "!", "Logout", "Login", routeValues: null, htmlAttributes: new { title = "Manage" })
                </li>
                <li><a href="~/Login/Logout">Log off</a></li>
            </ul>
        }
    }
    else
    {
        <ul class="nav navbar-nav navbar-right">
            <li>@Html.ActionLink("Register", "Index", "Register", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li>@Html.ActionLink("Log in", "Index", "Login", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        </ul>
    }
*@

@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", new { area = "" }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
@Html.AntiForgeryToken()
                   @* <div class="ms_top_right">
                        <div class="ms_top_lang">
                            <span data-toggle="modal" data-target="#lang_modal">languages <img src="/Musicstoreimages/svg/lang.svg" alt=""></span>
                        </div>
                        <div class="ms_top_btn">
                            <a href="javascript:;" class="ms_btn reg_btn" data-toggle="modal" data-target="#myModal"><span>@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })</span></a>
                            <a href="javascript:;" class="ms_btn login_btn" data-toggle="modal" data-target="#myModal1"><span><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></span></a>
                        </div>
                    </div> 
                    
                   *@
                    <div class="ms_header">
                        <div class="ms_top_left">
                            <div class="ms_top_search">
                                <span><a href="#" class="ms_color"><font size="4">Admin Panel - Music Store</font></a></span>
                            </div>
                        </div>
                        <div class="ms_top_right">
                            <div class="ms_top_btn">
                                <a href="javascript:document.getElementById('logoutForm').submit()" class="ms_btn login_btn">Log off</a>
                                
                            @*    <a href="@Url.Action("LogOff", "Account", new { Area = "" })" class="ms_btn login_btn"><span>Logout</span></a>
                            *@
                            </div>
                        </div>
                    </div>
    }
            }
            else
            { @*   <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
            *@
                            <div class="ms_top_right">
                               

                                <div class="ms_top_btn">
                                    <a href="javascript:;" id="modal-link" class="ms_btn reg_btn" data-toggle="modal" data-target="#myModal"><span>Register</span></a>
                                    <a href="javascript:;" class="ms_btn login_btn" data-toggle="modal" data-target="#myModal1"><span>Login</span></a>
                                </div>
                            </div>}
